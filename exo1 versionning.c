#include <stdio.h>
int main(){
    int i,x,n;
    do{
     printf("saisir n entiers:");
    scanf("%d", &n);
    }while(n<0);
      for(i=0;i<n;i++){
            do{
                 printf("veullez saisr un entier negatif:");
        scanf("%d", &x);
            }while(

        printf("le nombre que tu as saisi est %d ")
      }



return 0;
}
